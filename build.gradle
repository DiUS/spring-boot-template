apply plugin: 'idea'

allprojects {
    apply plugin: "idea"
    apply plugin: 'java'
}

subprojects {
    buildscript {
        repositories {
            maven { url "http://repo.spring.io/libs-snapshot" }
            mavenLocal()
            mavenCentral()
        }
    }
}